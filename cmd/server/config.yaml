prometheus:
  enabled: true

profiling:
  enabled: true
  port: 6060

logging:
  level: "info"

storage:
  gc_interval: 300
  gc_discard_ratio: 0.7

queue:
  acknowledgement_check_interval: 1
  acknowledgement_timeout: 60
